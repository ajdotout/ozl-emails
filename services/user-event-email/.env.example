SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"

# Optional: override the default realtime channel name
# REALTIME_CHANNEL="user_events_with_email"

# SparkPost integration (currently only logged, not actually sent)
SPARKPOST_API_KEY="your-sparkpost-api-key"
SPARKPOST_SENDER="notifications@example.com"

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL="INFO"